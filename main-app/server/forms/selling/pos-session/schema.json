{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string"
    },
    "employee": {
      "type": "string",
      "oneOf": "{{resolve:hr-service:employee}}"
    },
    "layout": {
      "type": "string",
      "oneOf": "{{resolve:selling-service:pos-layout}}"
    },
    "disabled": {
      "type": "boolean"
    },
    "status": {
      "type": "string"
    },
    "createdAt": {
      "type": "string",
      "format": "date-time"
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time"
    },
    "description": {
      "type": "string"
    },
    "startDate": {
      "type": "string",
      "format": "date-time"
    },
    "endDate": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": []
}
